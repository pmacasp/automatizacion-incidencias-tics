[pytest]
testpaths = tests
pythonpath = src
addopts = -ra

markers =
    unit: pruebas unitarias de funciones o clases aisladas
    integration: pruebas de integración entre módulos
    regression: pruebas para errores previamente corregidos
    e2e: pruebas del flujo completo de la aplicación
